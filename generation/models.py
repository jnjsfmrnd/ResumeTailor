# The GenerationRun model lives in resume_sessions/models.py alongside
# ResumeSession, ResumeSection, and CoverLetterDraft so that all domain
# models share a single migration history.
#
# Contracts: docs/contracts/generation-service-interface.md v1.0.0
